@model List<SanPham>

@*@if (Model != null)
{
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        <img src="/images/@item.SpHinhanh" height="80px" width="100px">
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.SpTensp)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.SpGia)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.SpSoluong)
                    </td>
                    <td style="width: 250px">
                        @Html.DisplayFor(modelItem => item.SpMota)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.LIdNavigation.LTenloai)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Ncc.NccTen)
                    </td>
                    <td>
                            <a asp-action="Edit" asp-route-id="@item.SpId" class="btn btn-warning">
                                <i class="fas fa-solid fa-pen"></i>
                            </a>
                            <a asp-action="Details" asp-route-id="@item.SpId" class="btn btn-info">
                                <i class="fas fa-info-circle"></i>
                            </a>
                            <a asp-action="Delete" asp-route-id="@item.SpId" class="btn btn-danger">
                                <i class="fas fa-trash"></i>
                            </a>
                        </td>
                </tr>
            }
}*@

    @if (Model != null)
    {
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <img src="/images/@item.SpHinhanh" height="80px" width="100px">
                </td>
                <td style="width:200px">
                    @Html.DisplayFor(modelItem => item.SpTensp)
                </td>
                <td>
                    @item.SpGia.ToString("#,##0") VNĐ
                </td>
               @* <td>
                    @Html.DisplayFor(modelItem => item.SpSoluong)
                </td>*@
                <td style="width: 250px">
                    @Html.DisplayFor(modelItem => item.SpMota)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LIdNavigation.LTenloai)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Ncc.NccTen)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.SpId" class="btn btn-warning">
                        <i class="fas fa-solid fa-pen"></i>
                    </a>
                    <a asp-action="Details" asp-route-id="@item.SpId" class="btn btn-info">
                        <i class="fas fa-info-circle"></i>
                    </a>
                    <a asp-action="Delete" asp-route-id="@item.SpId" class="btn btn-danger">
                        <i class="fas fa-trash"></i>
                    </a>
                </td>
            </tr>
        }
    }

